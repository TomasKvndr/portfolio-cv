
  # Revise CV Color Scheme

  This is a code bundle for Revise CV Color Scheme. The original project is available at https://www.figma.com/design/vXLGCPRH7u4cNLqn9Zopy7/Revise-CV-Color-Scheme.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  